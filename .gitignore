# ===========================================================================
# QEDLoop —— 哪些东西不进仓库
#
# 每条都写了「为什么」：忽略规则最容易变成一份没人敢动的祖传清单。
# ===========================================================================

# --- 运行产物（最重要的一条）------------------------------------------------
# runs/ 下是闭环每次运行的账本：trace.jsonl、state.json、REPORT.md、candidate/。
# 有三类东西不该公开：
#   1) **被测仓库的源码**——候选树与 diff 里带出的是目标仓库的代码片段。
#      即使两个仓库同作者，把 A 项目的代码复制进 B 项目，也会让许可证与
#      版权归属搅在一起；
#   2) **本机绝对路径**——C:\Users\<用户名>\...、D:\GitHubDocument\... 会随
#      trace、报告与证据脚本一起进仓库；
#   3) 真实的模型输出、token 花费与失败重试过程。
# 实测一个 runs/ 就有 4.1 MB / 313 个文件。注意：**事后再加忽略规则不会让
# 已经提交过的内容消失**，历史里的 blob 还在，所以这条要在第一次 git add 之前就位。
runs/

# --- 测试与静态检查的缓存 ---------------------------------------------------
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.tmp/
.coverage
coverage.xml
htmlcov/
*.junit.xml
junit*.xml

# --- 虚拟环境与打包产物 -----------------------------------------------------
.venv/
venv/
env/
*.egg-info/
dist/
build/

# --- 目标仓库的本地配置 -----------------------------------------------------
# targets/<目标 id>/loop.yml 写的是**这台机器的事实**：目标仓库的绝对路径
# （target: D:/GitHubDocument/EER-Ai）、本机环境变量名、本地渠道地址。
# 它不属于项目本身，别人 clone 下去也用不了。约定说明 targets/README.md 照常提交
# （下面这条只忽略子目录，不会连带忽略它）。
# 想让某个目标的配置成为公开示例，就把那一个目录单独放行，或挪进 examples/。
targets/*/

# --- 本地文档（每人一份，不进仓库）------------------------------------------
# CLAUDE.md 是「你对自己项目的约定」：里面会有本机路径、内部决定、实测过的失败记录。
# 仓库提交的是模板 CLAUDE.md.sample，使用者复制成 CLAUDE.md 之后由这条挡住。
# （`CLAUDE.md.sample` 名字不同，不受影响；根目录与子目录的 CLAUDE.md 都会命中。）
CLAUDE.md

# --- 密钥：设计上从不写进文件，这条是保险 -----------------------------------
# 渠道的 key 一律从**进程环境变量**读（providers.<name>.api_key_env），框架任何
# 地方都不落盘，报告只显示「有 / 没有 key」。但人会犯错，所以把常见密钥文件名
# 兜住——命中时不提交，比事后 git filter-repo 便宜得多。
.env
.env.*
!.env.example
*.key
*.pem
secrets.*
credentials.*

# --- apply 留下的备份 -------------------------------------------------------
# apply 默认在每个被覆盖的文件旁边留一份 .qedloop.bak。被测仓库通常在仓库之外，
# 但 examples/ 就在仓库里，跑一次演示的 apply 就会在那里留下这些文件。
*.qedloop.bak
*.orig
*.rej

# --- 编辑器与操作系统 -------------------------------------------------------
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db
desktop.ini
